#include <bits/stdc++.h>

class Pin {
public:
    // Variables
    std::string name;
    int x_cord;
    int y_cord;

    // Constructors
    Pin();
    Pin(std::string n, int x, int y);

    // Functions
    void print();
};
