#include <bits/stdc++.h>

class Row {
public:
    // Variables
    double x, y;
    int height;
    int num_sites;
    int site_width;

    // Constructors
    Row();
    Row(double x, double y, int height, int num_sites, int site_width);

    // Functions

    // Utils
    void print();
};
