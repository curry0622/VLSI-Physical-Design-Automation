#include <bits/stdc++.h>

class RowSection {
public:
    // Variables
    double x;
    int w;

    // Constructors
    RowSection();
    RowSection(double x, int w);

    // Functions
    void set_xw(double x, int w);

    // Utils
    void print();
};
