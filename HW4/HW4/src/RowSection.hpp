#include <bits/stdc++.h>

class RowSection {
public:
    // Variables
    double x;
    int w;

    // Constructors
    RowSection();
    RowSection(double x, int w);

    // Functions

    // Utils
    void print();
};
