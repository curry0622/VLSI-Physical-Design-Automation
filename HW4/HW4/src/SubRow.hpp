#include <bits/stdc++.h>

class SubRow {
public:
    // Variables
    double x;
    int w;

    // Constructors
    SubRow();
    SubRow(double x, int w);

    // Functions
    void set_xw(double x, int w);

    // Utils
    void print();
};
