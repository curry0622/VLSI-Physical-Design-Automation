#include <bits/stdc++.h>

class Legalizer {
public:
    // Variables

    // Constructors
    Legalizer();
    Legalizer(std::string input_file, std::string output_file);

    // Functions

    // Utils
};
